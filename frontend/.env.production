# Production environment variables
# These are baked into the build at build time on Vercel
# CRITICAL: Never use localhost in production

NEXT_PUBLIC_API_URL=https://litdocket-production.up.railway.app
NEXT_PUBLIC_WS_URL=litdocket-production.up.railway.app
